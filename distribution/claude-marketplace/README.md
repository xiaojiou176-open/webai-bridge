# WebaiBridge Claude-Compatible Companion Bundle

这不是 `WebaiBridge` 的主产品身份。
它更像一块已经裁好的“转接板”：

- 对 Claude Code 来说，它是 **marketplace-compatible companion bundle**
- 对 OpenClaw 来说，它是 **Claude-compatible later-lane bundle**
- 对当前 repo 来说，它只是 **第三层外包装**，排在 shared runtime 与
  `runtime-diagnostics` public skill packet 之后

当前最诚实的定位是：

- repo-owned companion bundle artifact
- marketplace-compatible bundle artifact
- later-lane host ecology material
- no official listing claimed yet
- no official listing or publication claimed yet

它不是：

- WebaiBridge 的 primary repo identity
- official marketplace listing
- release-published package
- full Claude Code parity
- full OpenClaw product-shell parity
