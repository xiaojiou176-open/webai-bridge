{
  "name": "webai-bridge-openclaw-host-example",
  "runtimeBaseUrlEnv": "WEBAI_BRIDGE_RUNTIME_BASE_URL",
  "catalogCommand": "pnpm run webai-bridge:cli -- host-example --target openclaw --json",
  "request": {
    "provider": "chatgpt",
    "lane": "web",
    "model": "gpt-4o",
    "input": "Return the current provider readiness summary before delegation."
  },
  "safeClaims": [
    "partial OpenClaw host example",
    "delegation-first config with preflight"
  ]
}
