{
  "name": "webai-bridge-codex-host-example",
  "runtimeBaseUrlEnv": "WEBAI_BRIDGE_RUNTIME_BASE_URL",
  "catalogCommand": "pnpm run webai-bridge:cli -- host-example --target codex --json",
  "request": {
    "provider": "chatgpt",
    "lane": "web",
    "model": "gpt-4o",
    "input": "Summarize the current provider health snapshot."
  },
  "safeClaims": [
    "partial Codex host example",
    "Responses-style runtime bridge config"
  ]
}
