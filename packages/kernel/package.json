{
  "name": "@webai-bridge/kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "pnpm exec node ../../scripts/build-package.mjs",
    "typecheck": "pnpm exec tsc -p tsconfig.json --pretty false",
    "test": "pnpm run test:unit",
    "test:unit": "pnpm exec vitest run --config ../../vitest.config.ts --project kernel"
  }
}
